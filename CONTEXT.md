# 仟仟自用插件

本上下文描述仟仟自用插件的业务语言，确保组件、LLM 工具和文档使用同一组概念。

## Language

**群专属头衔**：
QQ 群授予单个群成员、仅在该群内生效的专属称号。
_Avoid_：群昵称、管理员身份、MaiBot 内部称号

**请求者**：
在 QQ 群内明确请求修改本人或一位指定成员群专属头衔的群成员。
_Avoid_：调用者、操作者

**指定成员**：
请求者在明确设置请求中通过唯一 At，或通过当前群内唯一匹配的群昵称/QQ 昵称指定的头衔修改对象。
_Avoid_：LLM 提供的 QQ 号、跨群用户、模糊匹配用户

**授权请求者**：
运行时配置明确列出的 QQ 用户，或在“允许所有群成员修改他人头衔”开启时的任意当前群成员；只有授权请求者可以修改指定成员，修改本人不受此限制。
_Avoid_：通过昵称授权、把机器人群主身份当作请求者权限

**当前群**：
请求者发出设置请求的 QQ 群，也是群专属头衔变更唯一允许作用的群。
_Avoid_：目标群、指定群

**可用群**：
当前连接的机器人 QQ 账号拥有群主身份、因而允许使用群专属头衔功能的 QQ 群。
_Avoid_：机器人加入的群、配置白名单群

**明确设置请求**：
请求者直接要求把本人或一位指定成员的群专属头衔设置为指定文本，或明确授权 LLM 选取一个头衔并立即设置；仅讨论头衔不属于设置请求。
_Avoid_：相关话题、头衔讨论

**指定头衔**：
请求者在明确设置请求中亲自给出的群专属头衔文本。
_Avoid_：建议头衔、候选头衔

**At 指定**：
锚定请求消息中除机器人外恰好存在一个成员 At，插件直接使用该结构化 At 的成员 ID。
_Avoid_：从展示文本解析 At、让 LLM 复制 QQ 号、多成员猜测

**昵称指定**：
请求原文使用成员名字、简称或群内称呼但没有 At 指定时，LLM 可结合当前对话、记忆或黑话将称呼理解为规范成员名；插件再以完整群成员列表中的群昵称优先、QQ 昵称兜底进行精确唯一确认。
_Avoid_：LLM 直接提供 QQ 号、插件模糊匹配、重名任选一人

**二次确认**：
昵称指定在当前群精确命中多位成员时，本次不执行；LLM 要求用户重新发送明确请求并 At 唯一目标成员，由新消息的结构化 At 完成确认。
_Avoid_：沿用旧请求猜测、只回复“是”确认、把列表第一人作为目标

**生成头衔**：
请求者明确授权后，由 LLM 选取并立即设置的群专属头衔文本。
_Avoid_：未获授权的改写、普通建议

**头衔字数**：
群专属头衔最多占 6 个 UTF-16 单元；普通字符与数字通常占 1，常见 Emoji 通常占 2，最终仍以 QQ 接受结果为准。
_Avoid_：字节数、可见字符数

**静默成功**：
头衔设置成功后，Command 不发送用户可见回复；Tool 仍可向 LLM 返回内部执行结果。
_Avoid_：无执行结果、静默失败

### QQ 群复读

**可复读消息**：
QQ 群成员发送的、内容由文本、Emoji 或 QQ 表情组成且未超过内置长度上限的消息。命令、含 At 的消息、通知、空消息、解析失败消息、回复引用、合并转发及其他富媒体消息不属于可复读消息。
_Avoid_：任意群消息、富媒体消息

**相同复读内容**：
组件顺序、组件类型与组件内容均严格一致的可复读内容；不折叠空白、不忽略大小写，也不使用语义相似度。
_Avoid_：近似内容、同义表达

**复读队列**：
同一个 QQ 群内连续出现相同复读内容而形成的消息序列；至少出现两个不同成员才允许机器人参与。任何成员发送的不同消息都会结束队列，机器人的普通回复不影响队列。
_Avoid_：单条消息、聊天记录

**复读门槛**：
允许机器人参与复读队列所需的两个不同群成员；同一成员重复发送相同内容不会增加人数。
_Avoid_：消息总数、发送次数

**队列有效期**：
复读队列中相邻两条相同内容允许间隔的最长时间；超过该时间后，相同内容也会形成一条新队列。
_Avoid_：永久队列、聊天记录保留时间

**复读抽签**：
复读队列首次达到复读门槛时，按配置概率进行的一次参与判定；未抽中的队列不会因后续成员继续复读而再次抽签。
_Avoid_：逐消息抽签、重复抽签

**队列复读**：
机器人在复读队列达到参与条件后，原样发送该队列内容的行为；机器人在同一条队列中至多参与一次。
_Avoid_：随机偷句、LLM 仿写

**复读截断**：
机器人执行队列复读后，不再让触发消息进入常规 Command 或 LLM 流程。
_Avoid_：LLM 再次回复、双重回复

**启用群**：
允许使用 QQ 群复读功能的群；未配置群白名单时，所有 QQ 群均为启用群。
_Avoid_：机器人加入的群、头衔可用群
